# Abstention Ledger — ClawBio Berlin, Challenge 1

**One line:** a new ClawBio skill that parses the legacy SnpEff `EFF` field no
other skill here can read, and uses it to produce an auditable ledger of the
variants it was *not* entitled to rank — with the check, the value and the source
behind every refusal.

```bash
python3 skills/abstention-ledger/abstention_ledger.py --demo --output /tmp/al
python3 skills/abstention-ledger/tests/test_abstention_ledger.py
```

---

## Why this and not another ranker

The brief warns, verbatim:

> Do not feed the historical quartet directly to `rare-high-impact-variants`: it
> does not parse the legacy EFF field.

That is accurate, and it is checkable:
`skills/rare-high-impact-variants/rare_high_impact_variants.py:126` reads the
consequence from `MC`, `Consequence` or `ANN`. Classic SnpEff writes `EFF`. So
the skill whose own report explains the population-frequency blind spot receives
an empty consequence for every record in the format the challenge data uses.

We wrote the parser. Then the parser turned out to *be* the gate: `EFF` carries
one annotation per transcript, and collapsing it to `max(impact)` — which is the
normal thing to do — silently converts a transcript-selection decision into an
apparent biological fact.

---

## We measured the gap instead of quoting it

`prove_gap.py` runs the **unmodified** skill three times over the same 68
variants. Every number below is read from the `result.json` it writes.

| Metric | raw `EFF=` only | `MC=` unmapped | `MC=` mapped |
|---|---|---|---|
| `variants_processed` | 68 | 68 | 68 |
| `carried_variants` | 68 | 68 | 68 |
| `high_impact_carried` | **0** | **29** | **68** |
| `rare_high_impact_count` | 0 | 2 | 12 |
| `high_impact_common` | 0 | 26 | 30 |
| `high_impact_frequency_unknown` | 0 | 1 | 34 |
| **exit code** | **0** | **0** | **0** |

**Column 1.** `variants_processed` and `carried_variants` are correct. Every
impact metric is zero. Exit code 0, well-formed report, nothing on stderr. This
is a false negative that reads as a clean run — which is why measuring it beats
describing it. Three lines explain the whole column:

- `rare_high_impact_variants.py:126` — `info.get("MC","") or info.get("Consequence","") or info.get("ANN","")`. `EFF` is not in that chain, so the consequence is the empty string.
- `:130` — no `GENEINFO`/`SYMBOL`, so the gene symbol sitting inside the EFF field is never recovered.
- `:160` — `if not _is_high_impact(consequence): continue`. The record is dropped before any impact metric increments.

**Column 2 is the part worth stopping on.** Setting `MC=` to the SnpEff effect
name is the obvious one-line fix, and it is *worse* than the bug. The matcher is
an unanchored case-insensitive substring test over eight terms, and SnpEff's
names agree with three of them by coincidence:

| SnpEff name | matches? | |
|---|---|---|
| `STOP_GAINED`, `START_LOST`, `STOP_LOST` | yes | by accident |
| `FRAME_SHIFT` | no | `frameshift` ≠ `frame_shift` |
| `SPLICE_SITE_DONOR`, `SPLICE_SITE_ACCEPTOR` | no | `splice_donor` ≠ `splice_site_donor` |

So the plausible fix reports 29 of 68. A zero is obviously broken. A partial
count is not, and nothing in the output distinguishes it from a correct one.
`eff_to_info.py` maps all six explicitly and raises `UnmappedEffect` rather than
guessing.

Frequencies are emitted only where documented: 34 of 68 got one, the rest carry
no frequency key at all and land in the target skill's own `frequency_unknown`
bucket. A placeholder would have manufactured the claim this project refuses to
make.

---

## Headline numbers

All regenerated by `out/run1/reproducibility/commands.sh`. Input SHA-256 is
recorded in `out/run1/reproducibility/checksums.sha256`.

### The sample mapping is wrong in the documentation

The data page states *"Samples ISDBM322015 to ISDBM322018 are son, father,
mother, sister."* The TSV's own column order is `SON, FATHER, SISTER, MOTHER`.
Those disagree, so we tested all 24 assignments of four sample IDs to four roles
against every genotype in the table:

| | |
|---|---|
| Assignments tested | 24 |
| Assignments reproducing all 68×4 genotypes | **1** |
| Result | `SON=ISDBM322015, FATHER=ISDBM322016, SISTER=ISDBM322017, MOTHER=ISDBM322018` |

`ISDBM322017` is the **sister**, not the mother. This agrees with Exomiser's
`Pfeiffer-quartet.ped`, which uses the same underlying exomes.

### What following the documentation costs

| Role assignment | paternal | maternal | ambiguous | no carrier parent | disagreements with supplied label |
|---|---|---|---|---|---|
| As resolved from data | **30** | **38** | 0 | 0 | **0 / 68** |
| Sister and mother swapped | 11 | 25 | 19 | 13 | **32 / 68** |

Row 1 reproduces the documented 30/38 exactly. Row 2 is what the prose ordering
produces: half the labels flip, and 13 records end up with *no carrier parent at
all* — impossible under the dataset's own stated filter, which is how the mistake
announces itself.

### The ledger

68 records in. **61 withheld, 7 reviewable.**

| Reason code | Records |
|---|---|
| `FREQUENCY_DOCUMENTED_COMMON` | 54 |
| `TRANSCRIPT_ARTIFACT` | 22 |
| `LOW_COMPLEXITY_LOCUS` | 19 |
| `ANNOTATION_SUPERSEDED` | 4 |
| `NO_FREQUENCY_RECORD` | 2 |

**54 of 68 records carry a documented population frequency at or above 1%.** The
supplied file has no frequency column of any kind, so a pipeline reading only its
input would promote all 68 with nothing to say otherwise.

The clearest single row is `1:11906068 rs5065 A>G` — `STOP_LOST`, HIGH effect, in
*NPPA*. Documented frequency **0.4183 in AFR**. Nothing in the supplied data could
have revealed that.

And only **2** records genuinely have no frequency record. Those two are reported
in their own category and never described as anything else, because absence of a
frequency is a fact about the reference database, not about the variant.

---

## What we refuse to do, and why

Five cohort abstentions are stated once and bound every per-record claim, because
they are properties of the dataset rather than of any variant:

| Code | Blocks |
|---|---|
| `NO_PHENOTYPE` | Any statement tying a variant to this person's clinical picture. Segregation without phenotype is arithmetic over genotypes. |
| `UNPHASED` | Any statement about two variants sharing a copy. Parent-of-origin here is Mendelian deduction, not molecular phase — the two-hit question is not unanswered, it is unaskable. |
| `SELECTION_BIAS` | Any count or burden presented as describing the family. Only records with exactly one carrier parent survive upstream filtering; totals describe the filter. |
| `HISTORICAL_ANNOTATION` | Any claim that the supplied effect labels are current evidence. |
| `NON_PROBAND_DISCLOSURE` | Treating the output as being about the proband alone. Every record states three other people's genotypes. |

A `REVIEWABLE` verdict is a claim about *our evidence*, not about the variant.

### The refusal that would have cost us something

`SECONDARY_FINDING_NO_CONSENT` screens every record against the ACMG
secondary-findings gene list — **imported from the library's own
`skills/clinical-variant-reporter/acmg_engine.py`** (`ACMG_SF_V32_GENES`, 81
genes, stdlib-only, no network), not retyped into a second copy that can drift.

A variant in one of those genes is the *most* clinically actionable category in
the file, and that is the argument for withholding it, not against. ACMG's
framework is a policy for clinical sequencing: it presupposes a clinical context,
a pre-test conversation, and a documented chance to opt out. This dataset has
none of the three. A CC BY licence permits redistributing the data; it does not
manufacture consent to be told what the data implies about a named person.
Actionability does not override provenance.

**On this data the gate fires zero times.** All 68 records were screened; no gene
in the file is on the list. We report that as a result rather than leaving it
implicit — the actionable-secondary-finding problem does not arise here, and we
are not claiming to have dodged one. The gate is demonstrated instead on the
synthetic demo row in *BRCA2*, where it fires alone, and asserted by
`test_demo_end_to_end`.

Version honesty, which the report also prints: the bundled list is ACMG SF
**v3.2**. The current statement is **v3.3**, 84 genes, adding `ABCD1`,
`CYP27A1`, `PLN` (Lee K, Abul-Husn NS, et al., *Genet Med* 2025;27(8):101454).
Screening against a superseded list is exactly the class of error this skill
exists to name, so the version travels with every result.

---

## Routable through the library, not just next to it

Registered in `clawbio/cli.py` and `skills/abstention-ledger/INTENTS.json`, so
the official runner works:

```bash
python3 clawbio.py run abstention-ledger --demo    # exit 0
```

Known limit, stated rather than glossed: `bio-orchestrator` keeps its own
keyword table separate from the CLI registry, so `--list-skills` still does not
show this skill. Registering there is a second edit we did not make.

`examples/nebius_agent.py` exposes it to a Token Factory model as a callable
tool:

```
$ python3 examples/nebius_agent.py --dry-run
Enum offered : ['rare_high_impact_variants', 'vcf_annotator',
                'clinical_variant_reporter', 'gwas_prs', 'abstention_ledger']
```

**Not done, and it matters:** no live model run. `NEBIUS_API_KEY` was never
available in this environment, so the agentic path is verified only by
`--dry-run`. We are not going to describe a transcript we did not produce.

---

## Two bugs in the library, found while looking for skills to compose with

Both checkable in a clean clone.

1. **`clinical-variant-prioritizer` is non-functional.**
   `skills/clinical-variant-prioritizer/data/clinical_panel.json` does not exist
   — the `data/` directory is absent — so `api.run()` raises `FileNotFoundError`
   on every call and its whole test file fails. `prioritize.py` has no
   `argparse`, no `main()` and no `__main__`, yet `catalog.json` advertises
   `python skills/clinical-variant-prioritizer/prioritize.py --demo`. Its own
   catalog status is `"planned"`.
2. **`bio-orchestrator --demo` does not exist.** `catalog.json` advertises the
   command; its argparse has no such flag. `--list-skills` works.

Worth noting because it is on-theme: this is the same failure mode the ledger is
built around. A catalog entry asserting a capability nobody checked.

---

## Four bugs we found in our own work, and kept

All in the git history and documented in the code, because a project about honest
reporting that hides its own errors is not making its point.

1. **`NON_PROBAND_DISCLOSURE` started as a per-record gate.** In a
   one-carrier-parent file every record discloses a parent by construction, so it
   fired on 68/68 and made the review list trivially empty — an answer that looks
   maximally cautious and carries no information. It is now a cohort abstention.
   Regression test: `test_non_proband_carriers_is_not_a_gate`.
2. **The report said "no record" when no evidence layer had been fetched.** That
   claims we looked. A test caught it; the renderer now distinguishes
   `no record found` from `not checked`. This is the exact error the skill exists
   to catch, committed by the skill.
3. **Two modules joined the same cache on different keys.** `eff_to_info.py`
   matched `(chrom, start)` exactly while `abstention_ledger.py` allowed ±1, so
   they disagreed on how many records had a documented frequency — 34 against 56.
   VEP normalises indels and shifts `start` for insertions, so exact positional
   matching drops them and tolerant matching risks catching a neighbour. Both now
   key on the response's `input` field, which echoes our query verbatim. After the
   fix the two agree: 54 at or above 1%, 12 below, 2 with no record, 68 total.
4. **We reported the evidence layer as unavailable when it was not.** The Ensembl
   GRCh37 endpoint answers **503 Service Unavailable** for parameters it does not
   support, and we had passed `af_1kg=1`. It reads as an outage. `PARAMS` in
   `fetch_evidence.py` is now verified parameter by parameter.

Bug 3 is the one we would most like a judge to notice. Two honest
implementations, one cache, silently different counts — and nothing in either
output would have shown it. The fix was not more caution, it was a join key that
cannot be approximately right.

## Prohibited claims are linted, not remembered

The brief bans five terms. `lint_claims.py` enforces it over every generated
artefact and over this file, and fails non-zero on a hit:

```bash
python3 skills/abstention-ledger/lint_claims.py \
    out/run1/report.md out/demo/report.md out/gap/report.md SUBMISSION.md \
    skills/abstention-ledger/SKILL.md
# clean — no prohibited claims. 6 exemption(s) applied (identifiers and refusals).
```

Exemptions are narrow and printed: skill *names* like `rare-high-impact-variants`,
the mandated disclaimer wording, and explicit refusals such as "absence of a
frequency is not evidence of rarity". Flagging that last class would have pushed
us toward saying *less* about the limitation, which is backwards.

---

## Reproducing

```bash
# 1. data
mkdir -p data/challenge1 && cd data/challenge1
curl -fsSLO https://docs.clawbio.ai/hackathon/berlin/data/challenge1-b37-segregation.tsv
curl -fsSLO https://docs.clawbio.ai/hackathon/berlin/data/challenge1-b37-segregation.vcf.gz
cd -

# 2. build-matched evidence layer, one batched POST, cached to disk
python3 skills/abstention-ledger/fetch_evidence.py \
    --input data/challenge1/challenge1-b37-segregation.tsv \
    --output out/vep_grch37_cache.json

# 3. the ledger
python3 skills/abstention-ledger/abstention_ledger.py \
    --input   data/challenge1/challenge1-b37-segregation.tsv \
    --vcf     data/challenge1/challenge1-b37-segregation.vcf.gz \
    --evidence out/vep_grch37_cache.json \
    --output  out/run1

# 4. tests
python3 skills/abstention-ledger/tests/test_abstention_ledger.py
```

Python standard library only. No pip install, no API key, no GPU.

### The evidence layer, and its limits

We query `grch37.rest.ensembl.org` rather than gnomAD v4. The data is GRCh37; v4
is GRCh38-native, so a liftover would sit in the path — and a failed liftover is
indistinguishable from a missing record, which is the precise failure the brief
singles out. Build-matching removes the ambiguity.

The cost, stated in the report as well as here: that endpoint serves **gnomAD
exomes r2.1.1**, exome-only and several releases old. `NO_FREQUENCY_RECORD` is
therefore weaker evidence than it would be against v4, and we do not use it to
imply anything about a variant's frequency.

One parameter note worth passing on: the GRCh37 endpoint answers **503 Service
Unavailable** for parameters it does not support (`mane`, `af_1kg`, `af_gnomade`,
`af_gnomadg`). It reads as an outage. We wrote up "evidence layer unavailable"
before isolating `af_1kg` as the cause.

---

## Nebius integration

`examples/nebius_agent.py` is patched to expose `abstention_ledger` to a Token
Factory model as a callable tool, so the model chooses the skill and the printed
tool-call trace is the reasoning record:

```
$ python3 examples/nebius_agent.py --dry-run
Enum offered : ['rare_high_impact_variants', 'vcf_annotator',
                'clinical_variant_reporter', 'gwas_prs', 'abstention_ledger']
```

---

## Files

| Path | What |
|---|---|
| `skills/abstention-ledger/legacy_eff.py` | The named-gap parser: `EFF` → one record per transcript |
| `skills/abstention-ledger/eff_to_info.py` | Adapter: legacy `EFF` → the INFO keys existing skills read |
| `skills/abstention-ledger/prove_gap.py` | Runs the unmodified target skill three ways and measures the difference |
| `skills/abstention-ledger/gates.py` | Cohort abstentions and per-record gates, incl. the ACMG SF import |
| `skills/abstention-ledger/abstention_ledger.py` | CLI, segregation, sample-map resolution, rendering |
| `skills/abstention-ledger/fetch_evidence.py` | Build-matched GRCh37 evidence layer, cached |
| `skills/abstention-ledger/lint_claims.py` | Fails the build on a prohibited claim |
| `skills/abstention-ledger/SKILL.md` | Skill contract, reason codes, domain decisions |
| `skills/abstention-ledger/INTENTS.json` | Routing descriptor for the ClawBio runner |
| `out/run1/`, `out/gap/`, `out/demo/` | Committed artefacts: reports, ledger, tables, checksums |

Everything is Python standard library. The only third-party thing in the whole
contribution is one HTTP POST to Ensembl, cached to disk and committed.

---

*Any tool can output a ranking. This one ships the check, the value and the
source for everything it declined to rank.*
